---
import CodeButton from "./CodeButton.astro";

const { id } = Astro.props;

const projects = [
  {
    section: "FULLSTACK",
    title: "FDF - Wireframe Viewer",
    url: "https://github.com/Javierub32/42-cursus/tree/main/Rank_02/FDF",
    imageUrl: "/fdf.webp",
    tags: ["C", "Makefile", "MinilibX"],
    description:
      "Proyecto de visualización 3D que renderiza mapas de altura. Implementa el algoritmo de <strong>Bresenham</strong> para el trazado de líneas y transformaciones de matriz para proyección isométrica.<br>El proyecto incluye rotación, zoom y traslación interactiva del modelo 3D, así como <strong>gradientes de color</strong> basados en la altura del terreno.",
  },
  {
    section: "ALGORITMOS",
    title: "Push Swap - Sorting Algorithm",
    url: "https://github.com/Javierub32/42-cursus/tree/main/Rank_02/Push_Swap",
    imageUrl: "/pushswap.webp",
    tags: ["C", "Makefile"],
    description:
      "Algoritmo de ordenación eficiente usando dos stacks y un conjunto limitado de operaciones. Usa el <strong>Radix Sort</strong> para ordenar los elementos.<br> El proyecto consiste en <strong>minimizar el número de operaciones</strong> necesarias para ordenar cualquier conjunto de números, en vez de optimizar la complejidad temporal.",
  },
  {
    section: "DESARROLLO WEB",
    title: "Portfolio Personal",
    url: "https://github.com/Javierub32/Porfolio",
    imageUrl: "/portfolio.webp",
    tags: ["Astro", "Tailwind CSS", "JavaScript", "HTML"],
    description:
      "Portfolio personal desarrollado con Astro y Tailwind CSS, tiene un <strong>diseño responsive</strong>  y optimización de rendimiento. Incluye animaciones CSS personalizadas, navegación fluida, componentes reutilizables y un sistema de scroll animado. ",
  },
];
---

<div class="w-full bg-neutral-950">
  <section id={id} class="mx-auto min-h-screen max-w-5xl px-4 sm:px-6 lg:px-8">
    <header class="pt-20 mb-10">
      <p class="font-semibold text-blue-400 tracking-wide">PROYECTOS</p>
      <h2
        class="font-semibold text-gray-200 text-3xl md:text-4xl mt-2 tracking-tight mb-[-20px]"
      >
        Proyectos destacados
      </h2>
    </header>

    <div class="divide-y divide-white/10">
      {
        projects.map((p, i) => {
          const isOdd = i % 2 != 0;
          return (
            <article class="grid md:grid-cols-2 items-center gap-8 md:gap-12 py-10">
              {/* Texto */}
              <div
                class={`order-2 ${isOdd ? "md:order-2" : "md:order-1"} max-w-xl`}
              >
                <p class="uppercase text-[11px] font-semibold tracking-wider text-blue-400">
                  {p.section}
                </p>
                <h3 class="mt-1 text-2xl md:text-3xl font-semibold text-gray-100 tracking-tight">
                  {p.title}
                </h3>
                <div
                  class="mt-4 prose prose-invert mb-4 "
                  set:html={p.description}
                />
                <CodeButton url={p.url} />
                <ul class="mt-4 flex flex-wrap gap-2" aria-label="Tecnologías">
                  {p.tags.map((t) => (
                    <li class="rounded-full bg-white/5 px-3 py-1 text-sm text-gray-300 ring-1 ring-white/10 hover:bg-white/10 transition">
                      {t}
                    </li>
                  ))}
                </ul>
              </div>

              {/* Imagen */}
              <a
                href={p.url}
                target="_blank"
                rel="noopener"
                class={`order-1 ${isOdd ? "md:order-1 md:justify-self-start" : "md:order-2 md:justify-self-end"} block group w-full max-w-[520px]`}
              >
                <div class="relative overflow-hidden rounded-xl ring-1 ring-white/10 shadow-lg w-full mx-auto md:mx-0">
                  <img
                    src={p.imageUrl}
                    alt={p.title}
                    class="w-full h-full object-cover aspect-[16/10] transition-transform duration-300 group-hover:scale-[1.02]"
                  />
                </div>
              </a>
            </article>
          );
        })
      }
    </div>
  </section>
</div>
